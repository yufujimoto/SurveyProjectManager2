json.extract! consolidation, :id, :uuid, :name, :geographic_annotation, :temporal_annotation, :description, :flickr_photosetid, :project, :created_at, :updated_at
json.url consolidation_url(consolidation, format: :json)
